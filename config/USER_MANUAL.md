# 📖 THE EMPIRE CHRONICLES: TERMINAL OPERATOR MANUAL

> *"In the silence of the void, command line is king. Here lies the rigorous documentation for the Empire Terminal v2.5.0 — The God Mode Interface."* — **Codex**

---

## 🏛️ SECTION I: THE GATEKEEPER

The Empire Terminal is not merely a shell; it is a gateway to the **Core Neural Network**. Access to high-level functions is restricted to authorized Travelers.

### 🔑 The Admin Protocol (Server God Mode)
To access the specific **God Mode Arsenal**, one must bypass the security layer.

1.  **Initiate Protocol**: Type `server` or `server start` in the terminal.
2.  **The Challenge**: A secure login overlay will intercept the request.
3.  **The Passphrase**: Enter `Travelers` to verify your identity.
4.  **Persistence**: Once unlocked, the gate remains open for the duration of the session. You may close and reopen the panel freely.

> **WARNING**: Multiple failed attempts will log the incident to the Sentinel Daemon.

---

## ⚡ SECTION II: GOD MODE ARSENAL

Behind the Traveler Gate lies the **Server Config Panel**, a dashboard of 12 legendary capabilities.

| Protocol | Designation | Effect |
| :--- | :--- | :--- |
| **SWARM** | `SWARM SCALE` | Deploys 100,000 bot instances. |
| **MIRROR** | `MIRROR CLONE` | Creates a full system backup to `us-central1-a`. |
| **KILLSWITCH** | `KILLSWITCH ALL` | Emergency halt of all trading processes. |
| **VERTEX** | `VERTEX IGNITE` | Activates Gemini 3 Pro Cluster integration. |
| **VAULT** | `VAULT OPEN` | Mounts the secure 10TB storage volume. |
| **TIMEWARP** | `TIME WARP` | Compresses 30 days of backtesting into 30 seconds. |
| **ARENA** | `ARENA HOST` | Launches a PvP Trading Tournament. |
| **POSSESS** | `GROK POSSESS` | Allows xAI Agent to take manual control. |

> *Use these tools with extreme caution. The 'Orbital Strike' capability is not a toy.*

---

## 🛠️ SECTION III: TERMINAL TOOLS

For the refined operator, the terminal offers a suite of customization tools hidden within the **Settings Module**.

*   **Access**: Click the `Settings` (Gear Icon) in the terminal header.
*   **Visuals**:
    *   **Font Size**: Slider (8px - 32px) for optimal readability.
    *   **Opacity**: Slider (50% - 100%) for that cyberpunk glass aesthetic.
*   **Toggles**:
    *   `WRAP`: Toggle line wrapping for long log streams.
    *   `TIME`: Toggle millisecond-precision timestamps.
    *   `SCROLL`: Lock/Unlock auto-scrolling.
*   **Utilities**:
    *   📷 **Screenshot**: Captures the current display to `~/empire-dropbox/screenshots/`.
    *   📡 **Cast**: WebRTC stream to remote observers (Network ID: #88392).

---

## ⌨️ SECTION IV: KEYBOARD SHORTCUTS

Efficiency is the currency of the Empire.

*   `Arrow Up / Ctrl + Up`: Navigate Command History (Previous).
*   `Arrow Down`: Navigate Command History (Next).
*   `Ctrl + F`: **Search Protocol**. Instantly grep through session logs.
*   `Right Click`: Access the **Context Menu** (Copy, Paste, Clear).

---

## 📜 APPENDIX A: TROUBLESHOOTING

*   **"System Offline"**: Check `server.py` on port 8000. Use the **Local Override** input in God Mode to change ports.
*   **"Access Denied"**: Ensure you are using the `Travelers` passphrase. Case sensitive.
*   **"Build Error"**: If Vite fails on build, verify `EmpireTerminal.jsx` closing tags are intact. (Patched v2.5.1).

---

> *"May your latency be low and your profits high."* — **End of File**
