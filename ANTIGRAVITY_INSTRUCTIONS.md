# ANTIGRAVITY AGENT INSTRUCTIONS
# PRIORITY: ALPHA-1
# TARGET: TEAM DEPLOYMENT

## [0] CORE DIRECTIVE
The goal is to UNIFY the Empire Terminal into a single, unbreakable "supply chain" where agents (SWARM) feed logic into squares (MODULES) which dock into the grid (LEGION).

## [1] CURRENT BATTLEFIELD
- **Terminal**: `empire_terminal_2_extracted/src/components/EmpireTerminal.jsx`
- **Grid Head**: `empire_terminal_2_extracted/src/components/organisms/LegionGrid.jsx` -- *The "CMD CTR" found in the tab bar.*
- **Factory Floor**: `empire_terminal_2_extracted/squares/` -- *Where raw squares are forged.*
- **Supply Line**: `empire_terminal_2_extracted/src/components/organisms/LiveMarket/` -- *Where finalized modules live.*

## [2] TEAM ASSIGNMENTS
**STATUS UPDATE:** All units active. VM Workers (VMe's) are OFFLINE/UNAVAILABLE.
When the team arrives (10 minutes), they must execute the following patterns:

### AGENT A (THE ARCHITECT)
- **Role**: Logic & Design
- **Action**: Locate the `squares/` directory. Forge new React components (`.jsx`) using the "SwarmHeatmap" or "EmpireBox" template.
- **Rule**: Every square MUST export a `ResizeObserver` (Sizability Function) and report dimensions.

### AGENT B (THE COURIER)
- **Role**: Transport & Integration
- **Action**: Move forged squares from `squares/` to `src/components/organisms/LiveMarket/`.
- **Action**: Register the new file in `LegionGrid.jsx`.
  1. Import it at the top.
  2. Add it to the `layouts` state (Batch 9, Batch 10, etc.).
  3. Render it inside the `<ResponsiveGridLayout>`.

### AGENT C (THE PROOF)
- **Role**: Testing & Logs
- **Action**: Ensure `console.log("[SQUARE_NAME] Payload: ...")` is firing.
- **Action**: Verify the "DIM: WxH" indicator is visible in the UI.

## [3] NETWORKING (THE REAR GUARD)
- **Status**: The "NET" tab is currently unstable.
- **Order**: Stand by for replacement. Do not fix. It will be swapped out when the 12 agents monopolize the browser stack.

## [4] SIGNAL
- When sending instructions, use markdown code blocks.
- **Passphrase**: "ABSOLUTE ENDGAME"

---
*Generated by Antigravity*
